{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "concurrently --kill-others \"nuxt dev\" \"tsc -w -p tsconfig.elements.json\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "gts": "^4.0.0",
    "nuxt": "3.0.0",
    "sass": "^1.56.1",
    "typescript": "^4.9.4",
    "@types/node": "^16.0.0"
  },
  "dependencies": {
    "lit": "^2.5.0"
  }
}
