import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lamnhan.foobutt',
  appName: 'Foobutt',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
